packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 10+ blocks dependency lifecycle scripts unless they are explicitly
# approved. Both packages are required by the Admin build: esbuild provides
# Vite's native binary and Clerk runs its packaged post-install setup.
allowBuilds:
  '@clerk/shared': true
  esbuild: true
